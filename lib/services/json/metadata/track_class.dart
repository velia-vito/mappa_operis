part of '../json.dart';

/// Annotation to mark a class for serialization.
class TrackClass {
  /// Function to re-serialize the object.
  // final Function reSerialize;

  /// Function to de-serialize the object.
  // final Function deSerialize;

  /// Annotation to mark a class for serialization.
  // const TrackClass(this.reSerialize, this.deSerialize);
  const TrackClass();
}
